# Crítico independiente sobre `5647275..2224afd` — 2026-09-05

Revisión adversaria de los tres commits que cerraron los diez riesgos, hecha
por un agente sin acceso a la conversación que los produjo, sólo al diff y al
repositorio. Sin modificar ficheros; suites rápidas en verde. Se anota entera,
con lo que se arregló y lo que se deja dicho: un crítico cuyos hallazgos se
filtran antes de anotarse no es independiente.

**Recuento: 0 P0 · 5 P1 · 10 P2** (dos P2 marcados PLAUSIBLE, sin verificar).

## P1 — los cinco, arreglados en el commit siguiente

| # | Hallazgo | Qué se hizo |
|---|---|---|
| 1 | `fantasy_build.py` no llamaba a `regular_season()`: el modelo de bust, la validación publicada y el bloque de especialistas sumaban playoffs (NE con 21 partidos en el payload). `availability.py` y `rookies.py` filtraban a mano | Filtro ÚNICO al cargar; los dos módulos y cinco scripts de validación llaman a `regular_season()`; `test_nobody_filters_the_season_stage_by_hand` lee el código; board regenerado; drill 15 |
| 2 | `claim_from_item` emparejaba `players[0]` con `player_ids[0]` por posición aunque `attach()` hubiera dejado la lista de ids más corta: Hendrickson con el id de Burrow, RESOLVED | Sólo se empareja con un id por nombre; si no, sin id y lo resuelve el índice; test y drill 16 |
| 3 | `_key` caía a `nombre@equipo` para AMBIGUOUS: dos «B.Robinson» se supersedían | Clave `None`; el test que lo bendecía corregido |
| 4 | `parse_publication_date` rellenaba el día que faltaba con el reloj de la máquina («September 2026» → hoy); «4-9-2026» con guiones se adivinaba; zonas ET/PT caían a UTC | Doble lectura con dos rellenos; guiones y puntos tan prohibidos como barras; `tzinfos` con las zonas de la prensa; 18 casos nuevos; drill 17 |
| 5 | `games.csv` viene de `raw.githubusercontent.com`, que no manda `Last-Modified`, con `force=True`: cada cambio de una línea lo fechaba con la descarga | Fecha del último commit por la API de GitHub; identidad de bytes también toma la fecha de origen si se conoce; sin respuesta, hora de descarga y AVISO por stderr. Desde el contenedor de desarrollo la API no contesta: `markets` sigue fechado con la descarga hasta el refresco en CI |

## P2 — arreglados

- Redondeo a cuatro decimales DESPUÉS de `kelly.decide`: un BET podía salir con `stake_fraction: 0.0`. La fracción se redondea a seis.
- «minimum edge 1.5 points … after shrinkage» en `/betting`: el mínimo se mide ANTES de encoger. Texto corregido.
- `_curve` del pateador sólo era monótona con `quad < 0`; con `quad > 0` y pendiente negativa bajaba antes del vértice. Plana hasta él.
- Un eco o un muro de pago con feed leído habrían salido PRODUCTION_INGESTIBLE: esas dos condiciones van antes de la técnica.
- `ui-numbers.mjs` no veía «49.3 / 50.9» en una lista con barras: dos cifras sin libro. Patrón ampliado y las dos con procedencia E4.
- `CLAUDE.md` citaba +107,6 para la brecha del novato; E25 mide +108,9 (123 pares).

## P2 — dichos, no arreglados

- `current_state` deja que un OFFICIAL antiguo gane a un REPORTED más nuevo sin límite de tiempo (PUP de marzo contra «activado» de septiembre). Es una decisión de diseño pendiente de preregistro, no un fallo de código: hoy no hay ninguna cadena así en el archivo.
- `market_snapshot.opening()` devuelve la primera línea OBSERVADA y la cabecera define OPEN como la primera PUBLICADA; `opened_at` y `phase_hint` no se usan; `closing_line_value` no comprueba `game_id`/lado. El módulo no tiene llamadores todavía: es el contrato para cuando haya datos de apertura, y se corrige al primer uso real.
- El drill 11 con `delta=0.0` no está en el borde exacto en coma flotante; el test de valores representables (0.125) es el que se pone rojo. Anotado en el propio test.
- No se puede establecer desde el repositorio que el job `browser` sea una comprobación REQUERIDA en la protección de rama: eso vive en la configuración de GitHub, no en `ci.yml`. Acción del dueño.
- `python-dateutil` sólo llega por pandas: se declara como dependencia directa en este commit.
- Los −0,42 → −0,11 de `kickers.py` (E8d, walk-forward) y los −0,66 → −0,39 de la web (E8c, bloque fijo) son dos diseños distintos que miden lo mismo; el preregistro lo explica y ahora también el docstring.
